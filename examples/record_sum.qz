record Position {
  x: int,
  y: int,
}

func main() {
  let p = Position {
    x: 10,
    y: 20,
  };
  let sum = (p: Position): int -> {
    p.x + p.y
  };

  println(p.x);
  println(p.y);
  println(sum(p));
}
