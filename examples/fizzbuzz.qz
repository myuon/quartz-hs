func main() {
  for i in range(1, 100) {
    if {
      (mod(i, 15) == 0) => {
        println("FizzBuzz");
      },
      (mod(i, 3) == 0) => {
        println("Fizz");
      },
      (mod(i, 5) == 0) => {
        println("Buzz");
      },
      true => {
        println(i);
      },
    }
  }
}
